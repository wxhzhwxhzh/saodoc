---
id: intro6
permalink: /js/jqueryselector
title: '🔵 jQuery选择器'
---

# jQuery 选择器详解

下面给你 **jQuery 选择器详解表格版（Markdown 格式）**，内容清晰、全面、可直接复制使用。

---


## 基础选择器

| 选择器                    | 示例            | 描述             |
| ---------------------- | ------------- | -------------- |
| `*`                    | `$(' * ')`    | 选择所有元素         |
| `#id`                  | `$('#box')`   | 选取指定 ID 的元素    |
| `.class`               | `$('.item')`  | 选取指定 class 的元素 |
| `element`              | `$('div')`    | 选择指定标签名的元素     |
| `selector1, selector2` | `$('div, p')` | 同时选择多个元素       |

---

## 层级（层次）选择器

| 选择器                   | 示例             | 描述                    |
| --------------------- | -------------- | --------------------- |
| `ancestor descendant` | `$('div p')`   | 选择 `div` 中所有后代 `p`    |
| `parent > child`      | `$('ul > li')` | 选择 `ul` 的直接子元素 `li`   |
| `prev + next`         | `$('h1 + p')`  | 选择紧跟在 `h1` 后面的第一个 `p` |
| `prev ~ siblings`     | `$('h1 ~ p')`  | 选择 `h1` 后面所有兄弟 `p` 元素 |

---

## 属性选择器

| 选择器             | 示例                    | 描述                            |         |                         |
| --------------- | --------------------- | ----------------------------- | ------- | ----------------------- |
| `[attr]`        | `$('[title]')`        | 选择含有此属性的元素                    |         |                         |
| `[attr=value]`  | `$('[type=text]')`    | 属性等于指定值                       |         |                         |
| `[attr!=value]` | `$('[type!=text]')`   | 属性不等于指定值                      |         |                         |
| `[attr^=value]` | `$('[href^=https]')`  | 属性值 **以 value 开头**            |         |                         |
| `[attr$=value]` | `$('[src$=.png]')`    | 属性值 **以 value 结尾**            |         |                         |
| `[attr*=value]` | `$('[title*=hello]')` | 属性值包含 value                   |         |                         |
| `[attr          | =value]`              | `$('[lang                     | =en]')` | 属性值为 value 或以 value- 开头 |
| `[attr~=value]` | `$('[class~=box]')`   | 属性值为以空格分隔的词列表，选择其中含 value 的元素 |         |                         |

---

## 表单选择器

| 选择器         | 示例                     | 描述                    |
| ----------- | ---------------------- | --------------------- |
| `:input`    | `$(':input')`          | 选择所有表单元素              |
| `:text`     | `$(':text')`           | 所有文本框                 |
| `:password` | `$(':password')`       | 密码框                   |
| `:radio`    | `$(':radio')`          | 单选框                   |
| `:checkbox` | `$(':checkbox')`       | 复选框                   |
| `:submit`   | `$(':submit')`         | 提交按钮                  |
| `:reset`    | `$(':reset')`          | 重置按钮                  |
| `:button`   | `$(':button')`         | 普通按钮                  |
| `:file`     | `$(':file')`           | 文件选择框                 |
| `:selected` | `$('option:selected')` | 被选中的 `<option>`       |
| `:checked`  | `$(':checked')`        | 被选中的 checkbox 或 radio |
| `:disabled` | `$(':disabled')`       | 禁用元素                  |
| `:enabled`  | `$(':enabled')`        | 启用元素                  |

---

## 可见性选择器

| 选择器        | 示例              | 描述     |
| ---------- | --------------- | ------ |
| `:visible` | `$(':visible')` | 所有可见元素 |
| `:hidden`  | `$(':hidden')`  | 所有隐藏元素 |

---

## 内容过滤选择器

| 选择器               | 示例                         | 描述          |
| ----------------- | -------------------------- | ----------- |
| `:contains(text)` | `$('p:contains("Hello")')` | 包含指定文本的元素   |
| `:empty`          | `$('div:empty')`           | 无子元素的元素     |
| `:has(selector)`  | `$('div:has(p)')`          | 含有指定选择器子元素  |
| `:parent`         | `$('div:parent')`          | 包含子元素或文本的节点 |

---

## 子元素过滤选择器

| 选择器                  | 示例                          | 描述               |
| -------------------- | --------------------------- | ---------------- |
| `:first-child`       | `$('p:first-child')`        | 首个子元素            |
| `:last-child`        | `$('p:last-child')')`       | 最后一个子元素          |
| `:nth-child(n)`      | `$('li:nth-child(2)')`      | 第 n 个子元素（从 1 开始） |
| `:nth-last-child(n)` | `$('li:nth-last-child(1)')` | 倒数第 n 个子元素       |
| `:only-child`        | `$('p:only-child')`         | 唯一子元素            |

---

## 基本过滤选择器（索引选择器）

| 选择器      | 示例              | 描述                |
| -------- | --------------- | ----------------- |
| `:first` | `$('li:first')` | 第一个元素             |
| `:last`  | `$('li:last')`  | 最后一个元素            |
| `:eq(n)` | `$('li:eq(2)')` | 索引为 n 的元素（从 0 开始） |
| `:gt(n)` | `$('li:gt(2)')` | 索引大于 n 的元素        |
| `:lt(n)` | `$('li:lt(2)')` | 索引小于 n 的元素        |
| `:even`  | `$('li:even')`  | 偶数索引              |
| `:odd`   | `$('li:odd')`   | 奇数索引              |

---

## 表单状态选择器

| 选择器      | 示例            | 描述        |
| -------- | ------------- | --------- |
| `:focus` | `$(':focus')` | 当前获得焦点的元素 |

---

