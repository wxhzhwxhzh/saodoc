---
id: intro5
permalink: /js/jquery
title: '🔵 jQuery'
---

# jQuery 入门教程

## 目录

1. [jQuery 简介](#jquery-简介)
2. [安装与引入](#安装与引入)
3. [选择器](#选择器)
4. [DOM 操作](#dom-操作)
5. [事件处理](#事件处理)
6. [动画效果](#动画效果)
7. [AJAX 请求](#ajax-请求)
8. [实战示例](#实战示例)

---

## jQuery 简介

jQuery 是一个快速、简洁的 JavaScript 库，它简化了 HTML 文档遍历、事件处理、动画和 Ajax 交互。jQuery 的设计理念是"写得更少，做得更多"（Write Less, Do More）。

**核心优势：**
- 强大的选择器引擎
- 简化的 DOM 操作
- 优雅的事件处理
- 跨浏览器兼容性
- 丰富的插件生态

---

## 安装与引入

### 方法一：CDN 引入

```html
<!DOCTYPE html>
<html>
<head>
    <title>jQuery 示例</title>
    <!-- 引入 jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body>
    <h1>Hello jQuery!</h1>
    
    <script>
        // 确保 DOM 加载完成后再执行
        $(document).ready(function() {
            console.log("jQuery 已就绪！");
        });
        
        // 简写形式
        $(function() {
            console.log("这也是 ready 的简写！");
        });
    </script>
</body>
</html>
```

### 方法二：本地下载

从 [jQuery 官网](https://jquery.com/download/) 下载文件并引入：

```html
<script src="js/jquery-3.6.0.min.js"></script>
```

---

## 选择器

jQuery 选择器基于 CSS 选择器，让你能轻松选取 HTML 元素。

### 基础选择器

```javascript
// 元素选择器
$("p")          // 选择所有 <p> 元素
$("div")        // 选择所有 <div> 元素

// ID 选择器
$("#myId")      // 选择 id="myId" 的元素

// 类选择器
$(".myClass")   // 选择所有 class="myClass" 的元素

// 通配符选择器
$("*")          // 选择所有元素
```

### 层级选择器

```javascript
// 后代选择器
$("div p")      // 选择 div 内的所有 p 元素

// 子元素选择器
$("div > p")    // 选择 div 的直接子元素 p

// 相邻兄弟选择器
$("h1 + p")     // 选择紧跟 h1 后的 p 元素

// 所有兄弟选择器
$("h1 ~ p")     // 选择 h1 后的所有兄弟 p 元素
```

### 过滤选择器

```javascript
// 第一个和最后一个
$("li:first")       // 第一个 li 元素
$("li:last")        // 最后一个 li 元素

// 索引选择
$("li:eq(2)")       // 第 3 个 li 元素（索引从 0 开始）
$("li:gt(1)")       // 索引大于 1 的 li 元素
$("li:lt(3)")       // 索引小于 3 的 li 元素

// 奇偶选择
$("tr:odd")         // 奇数行
$("tr:even")        // 偶数行

// 可见性
$(":hidden")        // 所有隐藏元素
$(":visible")       // 所有可见元素

// 表单相关
$(":input")         // 所有 input 元素
$(":text")          // 所有 text 类型的 input
$(":checked")       // 所有选中的 checkbox 或 radio
$(":selected")      // 所有选中的 option
```

### 实战示例

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        .highlight { background-color: yellow; }
    </style>
</head>
<body>
    <ul id="myList">
        <li>项目 1</li>
        <li class="special">项目 2</li>
        <li>项目 3</li>
        <li>项目 4</li>
    </ul>
    
    <script>
        $(function() {
            // 选择第一个 li
            $("#myList li:first").addClass("highlight");
            
            // 选择带有 special 类的元素
            $(".special").css("color", "red");
            
            // 选择最后两个 li
            $("#myList li:gt(1)").css("font-weight", "bold");
        });
    </script>
</body>
</html>
```

---

## DOM 操作

### 获取和设置内容

```javascript
// 文本内容
$("p").text()                  // 获取文本
$("p").text("新文本")          // 设置文本

// HTML 内容
$("div").html()                // 获取 HTML
$("div").html("<b>粗体</b>")   // 设置 HTML

// 表单值
$("input").val()               // 获取值
$("input").val("新值")         // 设置值
```

### 操作属性

```javascript
// 获取和设置属性
$("img").attr("src")                    // 获取 src 属性
$("img").attr("src", "new.jpg")         // 设置 src 属性
$("img").attr({
    "src": "new.jpg",
    "alt": "新图片"
})                                       // 设置多个属性

// 移除属性
$("img").removeAttr("title")

// 操作 data 属性
$("div").data("key", "value")           // 设置 data-key
$("div").data("key")                    // 获取 data-key
```

### 操作 CSS 类

```javascript
// 添加类
$("p").addClass("highlight")
$("p").addClass("class1 class2")

// 移除类
$("p").removeClass("highlight")

// 切换类
$("p").toggleClass("active")

// 检查是否有某个类
if ($("p").hasClass("highlight")) {
    console.log("有 highlight 类");
}
```

### 操作 CSS 样式

```javascript
// 获取样式
$("p").css("color")

// 设置单个样式
$("p").css("color", "red")

// 设置多个样式
$("p").css({
    "color": "white",
    "background-color": "blue",
    "font-size": "20px"
})
```

### 添加和删除元素

```javascript
// 在末尾添加
$("ul").append("<li>新项目</li>")       // 内部末尾
$("ul").appendTo("<li>新项目</li>")     // 移动到末尾

// 在开头添加
$("ul").prepend("<li>新项目</li>")      // 内部开头
$("<li>新项目</li>").prependTo("ul")    // 移动到开头

// 在之后添加
$("p").after("<div>新内容</div>")       // 元素之后
$("<div>新内容</div>").insertAfter("p") // 插入到之后

// 在之前添加
$("p").before("<div>新内容</div>")      // 元素之前
$("<div>新内容</div>").insertBefore("p")// 插入到之前

// 删除元素
$("p").remove()                          // 删除元素及其所有内容
$("p").empty()                           // 清空元素内容
```

### 实战示例：动态列表

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        .completed { text-decoration: line-through; color: gray; }
    </style>
</head>
<body>
    <h2>待办事项</h2>
    <input type="text" id="taskInput" placeholder="输入任务">
    <button id="addBtn">添加</button>
    <ul id="taskList"></ul>
    
    <script>
        $(function() {
            // 添加任务
            $("#addBtn").click(function() {
                var task = $("#taskInput").val();
                if (task) {
                    $("#taskList").append(
                        '<li>' + task + 
                        ' <button class="deleteBtn">删除</button>' +
                        '</li>'
                    );
                    $("#taskInput").val("");
                }
            });
            
            // 点击任务标记完成
            $("#taskList").on("click", "li", function() {
                $(this).toggleClass("completed");
            });
            
            // 删除任务
            $("#taskList").on("click", ".deleteBtn", function(e) {
                e.stopPropagation();
                $(this).parent().remove();
            });
        });
    </script>
</body>
</html>
```

---

## 事件处理

### 基础事件

```javascript
// 点击事件
$("button").click(function() {
    alert("按钮被点击了！");
});

// 双击事件
$("p").dblclick(function() {
    $(this).css("color", "red");
});

// 鼠标进入和离开
$("div").mouseenter(function() {
    $(this).css("background-color", "yellow");
});

$("div").mouseleave(function() {
    $(this).css("background-color", "white");
});

// hover（进入和离开的简写）
$("div").hover(
    function() {
        $(this).addClass("hover");
    },
    function() {
        $(this).removeClass("hover");
    }
);
```

### 表单事件

```javascript
// 获得焦点
$("input").focus(function() {
    $(this).css("background-color", "#ffffcc");
});

// 失去焦点
$("input").blur(function() {
    $(this).css("background-color", "white");
});

// 值改变
$("input").change(function() {
    console.log("值已改变：" + $(this).val());
});

// 表单提交
$("form").submit(function(e) {
    e.preventDefault(); // 阻止默认提交
    console.log("表单提交被拦截");
});
```

### 事件绑定与委托

```javascript
// on() 方法（推荐）
$("button").on("click", function() {
    alert("点击事件");
});

// 绑定多个事件
$("input").on({
    focus: function() {
        $(this).css("background", "yellow");
    },
    blur: function() {
        $(this).css("background", "white");
    }
});

// 事件委托（用于动态元素）
$("#parent").on("click", ".child", function() {
    console.log("子元素被点击");
});

// 解绑事件
$("button").off("click");
```

### 事件对象

```javascript
$("a").click(function(event) {
    event.preventDefault();           // 阻止默认行为
    event.stopPropagation();         // 阻止事件冒泡
    
    console.log(event.type);         // 事件类型
    console.log(event.target);       // 触发事件的元素
    console.log(event.pageX);        // 鼠标 X 坐标
    console.log(event.pageY);        // 鼠标 Y 坐标
});
```

### 实战示例：下拉菜单

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        .menu { position: relative; display: inline-block; }
        .dropdown { display: none; position: absolute; background: #f9f9f9;
                    min-width: 160px; box-shadow: 0 8px 16px rgba(0,0,0,0.2); }
        .dropdown a { display: block; padding: 12px 16px; text-decoration: none; }
        .dropdown a:hover { background-color: #f1f1f1; }
    </style>
</head>
<body>
    <div class="menu">
        <button class="menu-btn">菜单 ▼</button>
        <div class="dropdown">
            <a href="#">选项 1</a>
            <a href="#">选项 2</a>
            <a href="#">选项 3</a>
        </div>
    </div>
    
    <script>
        $(function() {
            $(".menu-btn").click(function(e) {
                e.stopPropagation();
                $(".dropdown").toggle();
            });
            
            // 点击页面其他地方关闭菜单
            $(document).click(function() {
                $(".dropdown").hide();
            });
            
            // 点击下拉菜单项
            $(".dropdown a").click(function(e) {
                e.preventDefault();
                alert("选择了：" + $(this).text());
                $(".dropdown").hide();
            });
        });
    </script>
</body>
</html>
```

---

## 动画效果

### 显示和隐藏

```javascript
// 立即显示/隐藏
$("div").hide();
$("div").show();

// 带动画的显示/隐藏
$("div").hide(1000);              // 1 秒内隐藏
$("div").show("slow");            // 慢速显示（600ms）
$("div").toggle("fast");          // 切换显示/隐藏（200ms）

// 带回调函数
$("div").hide(1000, function() {
    alert("隐藏完成！");
});
```

### 淡入淡出

```javascript
// 淡入淡出
$("div").fadeIn(1000);            // 淡入
$("div").fadeOut(1000);           // 淡出
$("div").fadeToggle(1000);        // 切换淡入/淡出

// 淡入到指定透明度
$("div").fadeTo(1000, 0.5);       // 淡入到 50% 透明度
```

### 滑动效果

```javascript
// 滑动显示/隐藏
$("div").slideDown(1000);         // 向下滑动显示
$("div").slideUp(1000);           // 向上滑动隐藏
$("div").slideToggle(1000);       // 切换滑动
```

### 自定义动画

```javascript
// animate() 方法
$("div").animate({
    left: '250px',
    opacity: '0.5',
    height: '150px',
    width: '150px'
}, 1000);

// 链式动画
$("div")
    .animate({left: '250px'}, 1000)
    .animate({top: '100px'}, 1000)
    .animate({left: '0px'}, 1000)
    .animate({top: '0px'}, 1000);

// 使用预定义值
$("div").animate({
    height: 'toggle'
}, 1000);
```

### 动画控制

```javascript
// 停止动画
$("div").stop();                  // 停止当前动画
$("div").stop(true);              // 停止所有排队动画
$("div").stop(true, true);        // 停止并完成当前动画

// 延迟
$("div").delay(1000).fadeIn();    // 延迟 1 秒后淡入
```

### 实战示例：手风琴菜单

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        .accordion { width: 400px; }
        .accordion-header { background: #4CAF50; color: white; padding: 15px;
                           cursor: pointer; border: 1px solid #fff; }
        .accordion-header:hover { background: #45a049; }
        .accordion-content { padding: 15px; background: #f1f1f1;
                            display: none; border: 1px solid #ddd; }
    </style>
</head>
<body>
    <div class="accordion">
        <div class="accordion-header">第一部分</div>
        <div class="accordion-content">
            <p>这是第一部分的内容。可以包含任何 HTML 内容。</p>
        </div>
        
        <div class="accordion-header">第二部分</div>
        <div class="accordion-content">
            <p>这是第二部分的内容。点击标题可以展开和收起。</p>
        </div>
        
        <div class="accordion-header">第三部分</div>
        <div class="accordion-content">
            <p>这是第三部分的内容。只有一个部分可以展开。</p>
        </div>
    </div>
    
    <script>
        $(function() {
            $(".accordion-header").click(function() {
                // 关闭其他打开的内容
                $(".accordion-content").not($(this).next()).slideUp();
                
                // 切换当前内容
                $(this).next().slideToggle();
            });
        });
    </script>
</body>
</html>
```

---

## AJAX 请求

### 基础 AJAX

```javascript
// $.ajax() 完整写法
$.ajax({
    url: "https://api.example.com/data",
    type: "GET",
    dataType: "json",
    success: function(data) {
        console.log("成功：", data);
    },
    error: function(xhr, status, error) {
        console.log("错误：", error);
    },
    complete: function() {
        console.log("请求完成");
    }
});
```

### 简化方法

```javascript
// GET 请求
$.get("api/users", function(data) {
    console.log(data);
});

// POST 请求
$.post("api/users", {
    name: "张三",
    age: 25
}, function(data) {
    console.log(data);
});

// 获取 JSON
$.getJSON("api/data.json", function(data) {
    console.log(data);
});
```

### 完整示例

```javascript
// 提交表单数据
$("#submitBtn").click(function() {
    var formData = {
        name: $("#name").val(),
        email: $("#email").val()
    };
    
    $.ajax({
        url: "/api/submit",
        type: "POST",
        data: JSON.stringify(formData),
        contentType: "application/json",
        beforeSend: function() {
            $("#loading").show();
        },
        success: function(response) {
            alert("提交成功！");
            console.log(response);
        },
        error: function(xhr, status, error) {
            alert("提交失败：" + error);
        },
        complete: function() {
            $("#loading").hide();
        }
    });
});
```

### 实战示例：用户列表加载

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        #loading { display: none; color: #666; }
        .user-card { border: 1px solid #ddd; padding: 10px; margin: 10px 0; }
    </style>
</head>
<body>
    <h2>用户列表</h2>
    <button id="loadUsers">加载用户</button>
    <div id="loading">加载中...</div>
    <div id="userList"></div>
    
    <script>
        $(function() {
            $("#loadUsers").click(function() {
                $.ajax({
                    url: "https://jsonplaceholder.typicode.com/users",
                    type: "GET",
                    dataType: "json",
                    beforeSend: function() {
                        $("#loading").show();
                        $("#userList").empty();
                    },
                    success: function(users) {
                        users.forEach(function(user) {
                            var card = $('<div class="user-card">')
                                .html(
                                    '<h3>' + user.name + '</h3>' +
                                    '<p>Email: ' + user.email + '</p>' +
                                    '<p>Phone: ' + user.phone + '</p>'
                                );
                            $("#userList").append(card);
                        });
                    },
                    error: function() {
                        $("#userList").html('<p>加载失败，请重试。</p>');
                    },
                    complete: function() {
                        $("#loading").hide();
                    }
                });
            });
        });
    </script>
</body>
</html>
```

---

## 实战示例

### 完整项目：图片画廊

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>jQuery 图片画廊</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; padding: 20px; }
        
        .gallery { display: flex; flex-wrap: wrap; gap: 10px; }
        .gallery-item { cursor: pointer; overflow: hidden; 
                        width: 200px; height: 200px; }
        .gallery-item img { width: 100%; height: 100%; 
                           object-fit: cover; transition: transform 0.3s; }
        .gallery-item:hover img { transform: scale(1.1); }
        
        .modal { display: none; position: fixed; z-index: 1000;
                top: 0; left: 0; width: 100%; height: 100%;
                background: rgba(0,0,0,0.9); }
        .modal-content { position: relative; margin: 50px auto;
                        max-width: 90%; max-height: 90%; }
        .modal-content img { width: 100%; height: auto; }
        .close { position: absolute; top: 15px; right: 35px;
                color: #f1f1f1; font-size: 40px; font-weight: bold;
                cursor: pointer; }
        .prev, .next { cursor: pointer; position: absolute; top: 50%;
                      padding: 16px; color: white; font-weight: bold;
                      font-size: 20px; background: rgba(0,0,0,0.5); }
        .prev { left: 0; }
        .next { right: 0; }
        .prev:hover, .next:hover { background: rgba(0,0,0,0.8); }
    </style>
</head>
<body>
    <h1>jQuery 图片画廊</h1>
    
    <div class="gallery">
        <div class="gallery-item">
            <img src="https://picsum.photos/400/400?random=1" alt="图片 1">
        </div>
        <div class="gallery-item">
            <img src="https://picsum.photos/400/400?random=2" alt="图片 2">
        </div>
        <div class="gallery-item">
            <img src="https://picsum.photos/400/400?random=3" alt="图片 3">
        </div>
        <div class="gallery-item">
            <img src="https://picsum.photos/400/400?random=4" alt="图片 4">
        </div>
        <div class="gallery-item">
            <img src="https://picsum.photos/400/400?random=5" alt="图片 5">
        </div>
        <div class="gallery-item">
            <img src="https://picsum.photos/400/400?random=6" alt="图片 6">
        </div>
    </div>
    
    <div class="modal" id="imageModal">
        <span class="close">&times;</span>
        <span class="prev">&#10094;</span>
        <span class="next">&#10095;</span>
        <div class="modal-content">
            <img id="modalImage" src="" alt="">
        </div>
    </div>
    
    <script>
        $(function() {
            var currentIndex = 0;
            var images = $(".gallery-item img");
            
            // 点击缩略图打开模态框
            $(".gallery-item").click(function() {
                currentIndex = $(this).index();
                showImage(currentIndex);
                $("#imageModal").fadeIn(300);
            });
            
            // 关闭模态框
            $(".close").click(function() {
                $("#imageModal").fadeOut(300);
            });
            
            // 点击背景关闭
            $("#imageModal").click(function(e) {
                if (e.target === this) {
                    $(this).fadeOut(300);
                }
            });
            
            // 上一张
            $(".prev").click(function() {
                currentIndex = (currentIndex - 1 + images.length) % images.length;
                showImage(currentIndex);
            });
            
            // 下一张
            $(".next").click(function() {
                currentIndex = (currentIndex + 1) % images.length;
                showImage(currentIndex);
            });
            
            // 键盘控制
            $(document).keydown(function(e) {
                if ($("#imageModal").is(":visible")) {
                    if (e.key === "ArrowLeft") {
                        $(".prev").click();
                    } else if (e.key === "ArrowRight") {
                        $(".next").click();
                    } else if (e.key === "Escape") {
                        $(".close").click();
                    }
                }
            });
            
            // 显示图片
            function showImage(index) {
                var src = $(images[index]).attr("src");
                $("#modalImage").attr("src", src);
            }
        });
    </script>
</body>
</html>
```

---

## 总结

本教程涵盖了 jQuery 的核心知识点：

1. **选择器**：强大而灵活的元素选取方式
2. **DOM 操作**：轻松操作 HTML 元素和属性
3. **事件处理**：简化的事件绑定和管理
4. **动画效果**：丰富的动画和特效方法
5. **AJAX**：简化的异步数据交互

### 学习建议

- 从简单示例开始，逐步增加复杂度
- 多实践，尝试修改示例代码
- 结合实际项目需求进行学习
- 查阅 [jQuery 官方文档](https://api.jquery.com/) 获取更多信息
- 注意：现代前端开发更多使用原生 JavaScript 或框架（Vue、React），但 jQuery 仍在许多项目中使用

### 下一步

- 学习 jQuery 插件的使用
- 探索 jQuery UI 和其他扩展库
- 了解现代前端框架的差异
- 实践更复杂的项目案例

祝您学习愉快！